<resources>
    <style name="Theme.Klingon" parent="Theme.AppCompat.Light">
        <!-- Here we setting appcompat’s actionBarStyle -->
        <item name="actionBarStyle">@style/Theme.AppCompat.Light.DarkActionBar</item>

        <!-- ...and here we setting appcompat’s color theming attrs -->
        <!--
        <item name="colorPrimary">@android:color/red</item>
        <item name="colorPrimaryDark">@android:color/blue</item>
        -->

        <!-- The rest of your attributes -->
    </style>
</resources>
