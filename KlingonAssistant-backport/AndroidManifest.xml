<?xml version="1.0" encoding="utf-8"?>
<!--
/*
** Copyright (C) 2012 De'vID jonpIn (David Yonge-Mallo)
**
** Licensed under the Apache License, Version 2.0 (the "License");
** you may not use this file except in compliance with the License.
** You may obtain a copy of the License at
**
**     http://www.apache.org/licenses/LICENSE-2.0
**
** Unless required by applicable law or agreed to in writing, software
** distributed under the License is distributed on an "AS IS" BASIS,
** WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
** See the License for the specific language governing permissions and
** limitations under the License.
*/
-->
<!-- For the backport version, the versionCode must be lower than the
     corresponding regular version. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="org.tlhInganHol.android.klingonassistant"
        android:versionCode="68"
        android:versionName="2012.12.31b-backport">

    <!-- Minimum Android version 1.6 (4), maximum version 2.0.1 (6),
         targeted version 4.0.3 (15) (this disables compatibility behaviour). -->
    <uses-sdk android:minSdkVersion="4"
              android:maxSdkVersion="6"
              android:targetSdkVersion="15" />

    <application android:label="@string/app_name"
                 android:icon="@drawable/ic_ka">

        <!-- The default activity of the app; displays search results. -->
        <!-- BACKPORT: Theme is no title bar. -->
        <activity android:name=".KlingonAssistant"
                  android:theme="@android:style/Theme.NoTitleBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Receives the search request. -->
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
                <!-- No category needed, because the Intent will specify this class component. -->
                <!-- <category android:name="android.intent.category.DEFAULT" /> -->
            </intent-filter>

            <!-- Points to searchable meta data. -->
            <meta-data android:name="android.app.searchable"
                       android:resource="@xml/searchable" />

            <!-- Receives links from within an entry. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="vnd.android.cursor.item/org.tlhInganHol.android.klingonassistant" />
            </intent-filter>
        </activity>

        <!-- Displays the Preferences screen. -->
        <activity android:name=".Preferences"
                android:label="@string/menu_preferences">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.PREFERENCE" />
            </intent-filter>
        </activity>

        <!-- Displays the definition of a entry. -->
        <!-- BACKPORT: Theme is no title bar. -->
        <activity android:name=".EntryActivity"
                  android:theme="@android:style/Theme.NoTitleBar" />

        <!-- Provides search suggestions for words and their definitions. -->
        <provider android:name=".KlingonContentProvider"
                  android:authorities="org.tlhInganHol.android.klingonassistant.KlingonContentProvider" />

        <!-- Points to searchable activity so the whole app can invoke search. -->
        <meta-data android:name="android.app.default_searchable"
                   android:value=".KlingonAssistant" />

    </application>
</manifest>
